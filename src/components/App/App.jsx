import React from 'react';

import './App.scss';

const App = (props) => (
  <div className="AppWrapper">
    Test content
  </div>
);

App.propTypes = {
  // bla: PropTypes.string,
};

App.defaultProps = {
  // bla: 'test',
};

export default App;
